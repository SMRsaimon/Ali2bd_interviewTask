import React from 'react';
import styles from "../resource/css/Header.module.css"

const Header = () => {
    return (
        <div className={styles.navWrapper}>
            
        </div>
    );
};

export default Header;